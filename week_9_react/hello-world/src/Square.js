import React from "react";

const Square = () => {
  const boxStyle = {
    width: "200px",
    height: "200px",
    backgroundColor: "red",
  };

  return <div style={boxStyle} />;
};

export default Square;
