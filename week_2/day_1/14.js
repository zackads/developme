let even = (num) => {
  return num % 2 === 0;
};

console.log(even(1)); // false
console.log(even(2)); // true

// Zack's tests
console.log(even(0)); // true
console.log(even(-1)); // false
console.log(even(-2)); // true
